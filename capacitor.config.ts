import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personalapp.com',
  appName: 'Personal App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
